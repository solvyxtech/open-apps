slug: cross-platform-mobile
kind: curated
title: Cross-Platform Mobile Apps
description: Production apps shipping on both iOS and Android from a single codebase.
query:
  stacks: [react-native, flutter, capacitor, kmp]
  excludeStatuses: [archived]
ranking:
  preset: quality
editorial:
  lastReviewedAt: "2026-08-11"
  selectionNote: |
    Stack is one of `react-native`, `flutter`, `capacitor`, `kmp` (each
    ships to both iOS and Android from a single codebase). Archived
    records are excluded. Ranked by adoption
    (GitHub stars, log scale) multiplied by recent activity (releases,
    pushes and commits).
seo:
  index: true
  title: Cross-Platform Open Source Mobile Apps
  description: Open-source apps that ship to both iOS and Android from one codebase — Flutter, React Native and Kotlin Multiplatform in production.
